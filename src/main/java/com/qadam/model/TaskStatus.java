package com.qadam.model;

public enum TaskStatus {
    /** Created by a business, not visible in the catalog yet. */
    DRAFT,
    /** Manually published by the business, visible in the catalog and open for proposals. */
    PUBLISHED
}
